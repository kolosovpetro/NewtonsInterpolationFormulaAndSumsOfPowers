\begin{lemma}[Forward-Backward relation]
  \label{lem:forward-via-backward-differences}
  For non-negative integers $k,m$, and an arbitrary
  integer $t$
  \begin{align*}
    \Delta^{k} t^{m} = \nabla^{k} (t+k)^{m}.
  \end{align*}
\end{lemma}
