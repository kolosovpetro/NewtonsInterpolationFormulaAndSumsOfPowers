name: Build PDF

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  workflow_dispatch:

env:
  ACTIONS_RUNNER_DEBUG: false

jobs:
  build-pdf:
    uses: kolosovpetro/NewtonsInterpolationFormulaAndSumsOfPowers/.github/workflows/build-pdf-template.yml@main
    with:
      file_name: 'NewtonsInterpolationFormulaAndSumsOfPowers'
      enable_patch_version: true
      runs_on: 'ubuntu-latest'
      workflow_name: 'Build PDF'
      build_assets_path: |
        src/NewtonsInterpolationFormulaAndSumsOfPowers.tex
        src/NewtonsInterpolationFormulaAndSumsOfPowers.bbl
        src/NewtonsInterpolationFormulaAndSumsOfPowers.bib
        src/sections
        src/metadata
      working_directory_for_latex: 'src'
